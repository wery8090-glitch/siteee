import { AppShell, PageHeading, SectionLink } from "@/components/ChromaShell";
import { uiText, useLanguage } from "@/contexts/LanguageContext";
import { PurchaseDialog } from "@/components/PurchaseDialog";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { Activity, AlertTriangle, ArrowDownToLine, Check, Clock3, CreditCard, KeyRound, Languages, Monitor, MoreHorizontal, RefreshCw, Rocket, ShieldCheck, Sparkles, Trash2, Users, Wifi } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

function formatDate(value?: Date | string | null) { return value ? new Date(value).toLocaleDateString("en-GB", { day: "2-digit", month: "short", year: "numeric" }) : "—"; }
function formatRelative(value?: Date | string | null) { if (!value) return "Never"; const mins = Math.max(1, Math.round((Date.now() - new Date(value).getTime()) / 60000)); return mins < 60 ? `${mins} min ago` : `${Math.round(mins / 60)}h ago`; }
function DashboardFrame({ children }: { children: React.ReactNode }) { return <AppShell><div className="container max-w-[1220px] py-8 sm:py-10">{children}</div></AppShell>; }
function StatCard({ label, value, meta, icon: Icon, accent = false }: { label: string; value: string; meta: string; icon: React.ElementType; accent?: boolean }) { return <div className={`surface p-5 ${accent ? "border-primary/25 bg-primary/[.045]" : ""}`}><div className="flex items-center justify-between"><span className="text-xs text-muted-foreground">{label}</span><Icon className={`h-4 w-4 ${accent ? "text-primary" : "text-muted-foreground"}`} /></div><div className="mt-5 text-2xl font-semibold tracking-[-.04em]">{value}</div><div className="mt-1 text-xs text-muted-foreground">{meta}</div></div>; }

export function DashboardPage() {
  const { data, isLoading, refetch } = trpc.dashboard.summary.useQuery();
  const user = data?.user;
  const plan = data?.subscription?.plan;
  return <DashboardFrame><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><PageHeading eyebrow="Overview" title={`Welcome back${user?.name ? `, ${user.name.split(" ")[0]}` : ""}.`} description="Your Chroma control surface for account access, devices, and the latest client build." /><div className="flex flex-wrap gap-2"><a href="chroma-loader://auth" className="inline-flex h-10 items-center rounded-xl bg-primary px-4 text-sm font-bold text-[#10150c]">Open Loader</a><Button onClick={() => refetch()} variant="outline" className="h-10 w-fit rounded-xl border-white/10 bg-transparent text-muted-foreground hover:border-primary/40 hover:text-foreground"><RefreshCw className="mr-2 h-4 w-4" /> Refresh</Button></div></div>{isLoading ? <div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[1,2,3,4].map(i => <div key={i} className="h-32 animate-pulse rounded-2xl bg-white/[.04]" />)}</div> : <>{!data?.user && <div className="mt-8 flex gap-3 rounded-2xl border border-amber-300/20 bg-amber-300/[.05] p-5 text-sm text-amber-100/75"><AlertTriangle className="h-4 w-4 shrink-0 text-amber-300" />Account data is not available yet. Sign in again after the database session is established.</div>}<div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Subscription" value={plan?.name || "Free"} meta={data?.subscription ? `Expires ${formatDate(data.subscription.subscription.endsAt)}` : "No active plan"} icon={CreditCard} accent /><StatCard label="Devices" value={`${data?.devices.length || 0} / ${plan?.deviceLimit || 1}`} meta="Active bindings" icon={Monitor} /><StatCard label="Client version" value={data?.latestVersion?.version || "—"} meta={data?.latestVersion ? `Minecraft ${data.latestVersion.minecraftVersion}` : "Not published"} icon={Sparkles} /><StatCard label="Last activity" value={formatRelative(user?.lastSignedIn)} meta="Account session" icon={Clock3} /></div><div className="mt-6 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><div className="glass rounded-2xl p-6"><div className="flex items-start justify-between"><div><div className="eyebrow">Current access</div><h2 className="mt-2 text-xl font-semibold">{plan?.name || "Free access"}</h2><p className="mt-1 text-sm text-muted-foreground">{data?.subscription ? `Active until ${formatDate(data.subscription.subscription.endsAt)}` : "Connect a plan to unlock Loader access."}</p></div><span className={`rounded-full px-3 py-1 text-[10px] font-bold uppercase tracking-[.14em] ${data?.subscription ? "bg-primary/15 text-primary" : "bg-white/10 text-muted-foreground"}`}>{data?.subscription ? "Active" : "Pending"}</span></div><div className="mt-7 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-white/10 bg-white/[.03] p-4"><div className="text-xs text-muted-foreground">Device limit</div><div className="mt-2 text-lg font-semibold">{plan?.deviceLimit || 1}</div></div><div className="rounded-xl border border-white/10 bg-white/[.03] p-4"><div className="text-xs text-muted-foreground">Last version</div><div className="mt-2 text-lg font-semibold">{data?.latestVersion?.version || "—"}</div></div><div className="rounded-xl border border-white/10 bg-white/[.03] p-4"><div className="text-xs text-muted-foreground">Sessions</div><div className="mt-2 text-lg font-semibold">Short-lived</div></div></div><div className="mt-6 flex flex-wrap gap-3"><Link href="/dashboard/subscription" className="inline-flex h-10 items-center rounded-xl bg-primary px-4 text-sm font-bold text-[#10150c]">Manage subscription</Link><Link href="/dashboard/downloads" className="inline-flex h-10 items-center rounded-xl border border-white/10 px-4 text-sm font-semibold hover:border-primary/40">View downloads</Link></div></div><div className="surface p-6"><div className="flex items-center gap-2"><div className="grid h-9 w-9 place-items-center rounded-lg bg-primary/10 text-primary"><Wifi className="h-4 w-4" /></div><div><div className="text-sm font-semibold">System status</div><div className="text-xs text-muted-foreground">All core services</div></div></div><div className="mt-6 grid gap-3">{[["Website", "Operational"], ["Account", "Operational"], ["Loader API", "Operational"]].map(([name, status]) => <div key={name} className="flex items-center justify-between text-sm"><span className="text-muted-foreground">{name}</span><span className="text-xs text-primary">{status}</span></div>)}</div><Link href="/status" className="mt-6 inline-flex text-xs font-semibold text-primary">View status <span className="ml-1">→</span></Link></div></div><div className="mt-5 surface p-6"><div className="flex items-center justify-between"><div><div className="eyebrow">Devices</div><h2 className="mt-2 text-lg font-semibold">Your active installations</h2></div><SectionLink href="/dashboard/devices">Manage</SectionLink></div>{data?.devices.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2">{data.devices.slice(0, 2).map(device => <div key={device.id} className="flex items-center gap-4 rounded-xl border border-white/10 bg-white/[.025] p-4"><div className="grid h-10 w-10 place-items-center rounded-lg bg-primary/10 text-primary"><Monitor className="h-4 w-4" /></div><div className="min-w-0"><div className="truncate text-sm font-semibold">{device.name}</div><div className="mt-1 text-xs text-muted-foreground">{formatRelative(device.lastSeenAt)} · {device.status}</div></div></div>)}</div> : <div className="mt-5 rounded-xl border border-dashed border-white/10 p-6 text-center text-sm text-muted-foreground">No devices bound yet. The Loader link flow will appear here when a device requests access.</div>}</div></>}</DashboardFrame>;
}

export function SubscriptionPage() {
  const { data: plans = [], isLoading } = trpc.plans.list.useQuery();
  const { data: summary, refetch } = trpc.dashboard.summary.useQuery();
  const current = summary?.latestSubscription;
  const [key, setKey] = useState("");
  const [keyMessage, setKeyMessage] = useState("");
  const redeem = trpc.dashboard.redeemKey.useMutation({ onSuccess: result => { setKey(""); setKeyMessage(`Активировано: ${result.plan} на ${result.durationDays} дней.`); refetch(); }, onError: error => setKeyMessage(error.message) });
  return <DashboardFrame><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><PageHeading eyebrow="Billing" title="Subscription" description="Choose a plan on the purchase page, then activate a key from your account." /><Link href="/pricing" className="inline-flex h-10 items-center justify-center rounded-xl bg-primary px-4 text-sm font-bold text-[#10150c]">Buy another subscription</Link></div><div className="mt-8 grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className="glass rounded-2xl p-6"><div className="eyebrow">Current subscription</div><div className="mt-3 grid gap-4 sm:grid-cols-4"><div><div className="text-xs text-muted-foreground">Plan</div><div className="mt-1 font-semibold">{current?.plan.name || "FREE"}</div></div><div><div className="text-xs text-muted-foreground">Status</div><div className="mt-1 font-semibold text-primary">{current?.subscription.status === "active" && current.subscription.endsAt > new Date() ? "Active" : "Inactive"}</div></div><div><div className="text-xs text-muted-foreground">Ends</div><div className="mt-1 font-semibold">{formatDate(current?.subscription.endsAt)}</div></div><div><div className="text-xs text-muted-foreground">Source</div><div className="mt-1 font-semibold">{current?.subscription.provider || "Manual"}</div></div></div></div><div className="surface rounded-2xl p-6"><div className="eyebrow">Activation</div><h2 className="mt-2 text-lg font-semibold">Activate a subscription key</h2><p className="mt-2 text-sm text-muted-foreground">Keys are single-use and look like CHROMA-fKLD39DP-292F-f93KSo.</p><div className="mt-4 flex gap-2"><input value={key} onChange={event => setKey(event.target.value.toUpperCase())} placeholder="CHROMA-XXXXXXXX-XXXX-XXXXXX" className="h-10 min-w-0 flex-1 rounded-xl border border-white/10 bg-white/[.04] px-3 text-xs outline-none focus:border-primary/60" /><Button onClick={() => redeem.mutate({ key })} disabled={redeem.isPending || key.length < 20} className="h-10 rounded-xl bg-primary px-4 text-xs font-bold text-[#10150c]">{redeem.isPending ? "Checking…" : "Activate"}</Button></div>{keyMessage && <div className="mt-3 rounded-xl border border-primary/20 bg-primary/[.05] p-3 text-xs text-primary">{keyMessage}</div>}</div></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{isLoading ? [1,2,3].map(i => <div key={i} className="h-72 animate-pulse rounded-2xl bg-white/[.04]" />) : plans.filter(plan => plan.slug !== "free").map((plan, index) => <div key={plan.id} className={`surface flex flex-col p-6 ${index === 0 ? "border-primary/45" : ""}`}><div className="flex items-center justify-between"><div className="text-xs font-bold uppercase tracking-[.18em] text-muted-foreground">{plan.slug === "premium_beta" ? "PREMIUM + BETA" : plan.name}</div>{index === 0 && <span className="rounded-full bg-primary px-2 py-1 text-[9px] font-bold text-[#10150c]">POPULAR</span>}</div><div className="mt-5 text-3xl font-semibold">{plan.slug === "premium_beta" ? "290" : plan.slug === "premium" ? "200" : "100"} ₽ <span className="text-sm font-normal text-muted-foreground">from</span></div><p className="mt-3 min-h-12 text-sm leading-6 text-muted-foreground">{plan.slug === "premium_beta" ? "Early access to updates and new features." : plan.description}</p><div className="mt-6 grid flex-1 gap-3">{(plan.slug === "premium_beta" ? ["Early access to updates", "New features first", "Expanded support"] : JSON.parse(plan.features || "[]")).map((item: string) => <div key={item} className="flex gap-2 text-sm"><Check className="h-4 w-4 shrink-0 text-primary" />{item}</div>)}</div><Link href="/pricing" className="mt-7 inline-flex h-11 items-center justify-center rounded-xl bg-primary font-bold text-[#10150c] hover:bg-[#d0ff73]">Open purchase page</Link></div>)}</div></DashboardFrame>;
}
export function DevicesPage() { const { data: devices = [], isLoading, refetch } = trpc.devices.list.useQuery(); const revoke = trpc.devices.revoke.useMutation({ onSuccess: () => refetch() }); const link = trpc.devices.verifyLinkCode.useMutation({ onSuccess: () => { setCode(""); refetch(); }, onError: error => setLinkError(error.message) }); const [confirmId, setConfirmId] = useState<number | null>(null); const [code, setCode] = useState(""); const [linkError, setLinkError] = useState(""); return <DashboardFrame><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><PageHeading eyebrow="Identity" title="Devices" description="Each Loader installation should use a cryptographic key pair. Chroma stores the public key and can revoke access at any time." /><Button onClick={() => refetch()} variant="outline" className="h-10 w-fit rounded-xl border-white/10 bg-transparent text-muted-foreground hover:border-primary/40 hover:text-foreground"><RefreshCw className="mr-2 h-4 w-4" /> Refresh</Button></div><div className="mt-8 glass rounded-2xl p-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><div className="eyebrow">Loader link</div><h2 className="mt-2 text-lg font-semibold">Approve a device</h2><p className="mt-1 max-w-xl text-sm text-muted-foreground">Enter the one-time code shown by the Loader. It expires quickly and is consumed after a successful approval.</p></div><div className="flex w-full max-w-sm gap-2"><input value={code} onChange={event => { setCode(event.target.value.toUpperCase()); setLinkError(""); }} placeholder="CHRM-XXXXXX" maxLength={11} className="h-10 min-w-0 flex-1 rounded-xl border border-white/10 bg-white/[.04] px-3 text-sm outline-none placeholder:text-muted-foreground/60 focus:border-primary/60" /><Button onClick={() => link.mutate({ code })} disabled={link.isPending || code.length !== 11} className="h-10 rounded-xl bg-primary px-4 text-xs font-bold text-[#10150c]">{link.isPending ? "Checking…" : "Approve"}</Button></div></div>{linkError && <p className="mt-3 text-xs text-red-200">{linkError}</p>}</div><div className="mt-10 grid gap-4">{isLoading ? [1,2].map(i => <div key={i} className="h-28 animate-pulse rounded-2xl bg-white/[.04]" />) : devices.length ? devices.map(device => <div key={device.id} className="surface flex flex-col gap-5 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-xl bg-primary/10 text-primary"><Monitor className="h-5 w-5" /></div><div><div className="font-semibold">{device.name}</div><div className="mt-1 text-xs text-muted-foreground">Device #{String(device.id).padStart(5, "0")} · Added {formatDate(device.createdAt)}</div><div className="mt-2 text-xs text-muted-foreground">Last seen {formatRelative(device.lastSeenAt)}</div></div></div><div className="flex items-center gap-3"><span className={`rounded-full px-3 py-1 text-[10px] font-bold uppercase tracking-[.14em] ${device.status === "active" ? "bg-primary/15 text-primary" : "bg-white/10 text-muted-foreground"}`}>{device.status}</span>{confirmId === device.id ? <><Button onClick={() => revoke.mutate({ deviceId: device.id })} disabled={revoke.isPending} className="h-9 rounded-lg bg-red-400/15 px-3 text-xs font-semibold text-red-200 hover:bg-red-400/25">Confirm revoke</Button><Button onClick={() => setConfirmId(null)} variant="ghost" className="h-9 px-2 text-xs">Cancel</Button></> : <Button onClick={() => setConfirmId(device.id)} variant="outline" className="h-9 rounded-lg border-white/10 bg-transparent px-3 text-xs text-muted-foreground hover:border-red-300/40 hover:text-red-200"><Trash2 className="mr-2 h-3.5 w-3.5" /> Revoke</Button>}</div></div>) : <div className="surface p-12 text-center"><Monitor className="mx-auto h-8 w-8 text-primary/60" /><h3 className="mt-4 font-semibold">No bound devices</h3><p className="mt-2 text-sm text-muted-foreground">When the Loader link flow is enabled, pending device requests will be confirmed here.</p><div className="mx-auto mt-5 max-w-md rounded-xl border border-dashed border-white/10 p-4 text-left text-xs leading-5 text-muted-foreground">Link codes are one-time, short-lived, and stored as hashes. The Loader never receives database credentials.</div></div>}</div></DashboardFrame>; }

export function DownloadsPage() { const { data, isLoading } = trpc.clientInfo.downloadInfo.useQuery(); return <DashboardFrame><PageHeading eyebrow="Releases" title="Downloads" description="Authorized downloads are checked server-side against your account, subscription, device, and the selected client version." /><div className="mt-10">{isLoading ? <div className="h-64 animate-pulse rounded-2xl bg-white/[.04]" /> : data ? <div className="glass rounded-2xl p-7"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><div><div className="grid h-12 w-12 place-items-center rounded-xl bg-primary/10 text-primary"><ArrowDownToLine className="h-5 w-5" /></div><h2 className="mt-6 text-2xl font-semibold">Chroma Client {data.version}</h2><p className="mt-2 text-sm text-muted-foreground">Minecraft {data.minecraftVersion} · {data.fileName}</p></div><span className="rounded-full border border-amber-300/20 bg-amber-300/[.05] px-3 py-1 text-[10px] font-bold uppercase tracking-[.14em] text-amber-200">Needs storage signing</span></div><div className="mt-8 rounded-xl border border-white/10 bg-white/[.03] p-5 text-sm leading-6 text-muted-foreground">{data.reason} Add storage credentials and an admin-published version before making the download action available.</div><Button disabled className="mt-6 h-11 rounded-xl bg-primary/40 px-5 text-[#10150c]"><ArrowDownToLine className="mr-2 h-4 w-4" /> Download unavailable</Button></div> : <div className="surface p-12 text-center"><Sparkles className="mx-auto h-8 w-8 text-primary/60" /><h3 className="mt-4 font-semibold">No release published</h3><p className="mt-2 text-sm text-muted-foreground">An administrator must publish a client version before it can be authorized for download.</p></div>}</div></DashboardFrame>; }

export function SecurityPage() { return <DashboardFrame><PageHeading eyebrow="Account protection" title="Security" description="Keep access scoped, visible, and revocable." /><div className="mt-10 grid gap-5 md:grid-cols-2"><div className="surface p-6"><ShieldCheck className="h-5 w-5 text-primary" /><h2 className="mt-5 font-semibold">Server-side sessions</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Browser auth uses the managed OAuth session and HttpOnly cookie path. Loader credentials are intended to be short-lived.</p><div className="mt-6 flex items-center gap-2 text-xs text-primary"><Check className="h-4 w-4" /> No tokens in localStorage</div></div><div className="surface p-6"><KeyRound className="h-5 w-5 text-primary" /><h2 className="mt-5 font-semibold">Device key hygiene</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">A Loader installation should create an Ed25519 identity locally. The private key is never uploaded to Chroma.</p><div className="mt-6 flex items-center gap-2 text-xs text-primary"><Check className="h-4 w-4" /> Public-key binding model</div></div></div></DashboardFrame>; }
export function SettingsPage() {
  const { language, setLanguage } = useLanguage();
  const t = uiText[language];
  return <DashboardFrame><PageHeading eyebrow={t.settings} title={t.settings} description={language === "ru" ? "Настройте язык и базовые предпочтения интерфейса Chroma." : "Tune the language and core interface preferences for your Chroma workspace."} /><div className="mt-10 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><div className="glass rounded-2xl p-6 sm:p-7"><div className="flex items-start justify-between gap-4"><div><div className="eyebrow">{t.language}</div><h2 className="mt-2 text-xl font-semibold">{language === "ru" ? "Язык интерфейса" : "Interface language"}</h2><p className="mt-2 max-w-lg text-sm leading-6 text-muted-foreground">{t.saveHint}</p></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-primary/10 text-primary"><Languages className="h-5 w-5" /></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><button onClick={() => setLanguage("en")} className={`group rounded-xl border p-4 text-left ${language === "en" ? "border-primary/60 bg-primary/[.08]" : "border-white/10 bg-white/[.025] hover:border-white/20"}`}><div className="flex items-center justify-between"><span className="text-sm font-semibold">{t.englishNative}</span><span className={`h-4 w-4 rounded-full border-2 ${language === "en" ? "border-primary bg-primary" : "border-white/20"}`} /></div><span className="mt-1 block text-xs text-muted-foreground">{t.english}</span></button><button onClick={() => setLanguage("ru")} className={`group rounded-xl border p-4 text-left ${language === "ru" ? "border-primary/60 bg-primary/[.08]" : "border-white/10 bg-white/[.025] hover:border-white/20"}`}><div className="flex items-center justify-between"><span className="text-sm font-semibold">{t.russianNative}</span><span className={`h-4 w-4 rounded-full border-2 ${language === "ru" ? "border-primary bg-primary" : "border-white/20"}`} /></div><span className="mt-1 block text-xs text-muted-foreground">{t.russian}</span></button></div><div className="mt-5 flex items-center gap-2 text-xs text-primary"><Check className="h-4 w-4" />{t.saved}</div></div><div className="grid gap-5"><div className="surface p-6"><Sparkles className="h-5 w-5 text-primary" /><h2 className="mt-5 font-semibold">{t.appearance}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{t.appearanceHint}</p><div className="mt-5 flex items-center gap-2 rounded-xl border border-primary/20 bg-primary/[.05] px-3 py-2 text-xs text-primary"><span className="h-2 w-2 rounded-full bg-primary" /> Dark premium surface</div></div><div className="surface p-6"><Clock3 className="h-5 w-5 text-primary" /><h2 className="mt-5 font-semibold">{t.notifications}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{t.notificationsHint}</p><div className="mt-5 text-xs text-muted-foreground">{t.unavailable}</div></div></div></div></DashboardFrame>;
}
